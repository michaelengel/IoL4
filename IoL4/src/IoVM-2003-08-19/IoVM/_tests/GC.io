
write("this test will run forever. If working properly, the process memory size should stop growing\n")
n = 1
while(1, 
  Object clone
  n ++
/*
  if(n > 8000, 
    count = collectGarbage
    write("collected ", count, " objects\n"); 
    n <- 0
  )
*/
)